module.exports = {
  name: 'softmute',
  description: 'Softmute (remove roles then reassign) - basic placeholder',
  async execute(message) {
    return message.reply('softmute is not fully implemented — placeholder');
  }
};
