# Additional commands implemented (moderation, utility, fun) and API keys support

New ENV variables (optional):
- OPENWEATHER_KEY (OpenWeatherMap API key) — for !weather
- GIPHY_API_KEY (GIPHY API key) — for !gif

Scripts:
- npm run register-commands  # registers slash commands to your test guild

